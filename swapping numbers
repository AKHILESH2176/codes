#include<stdio.h>
int main()
{
int first,second,temp;
printf("enter first number\n");
scanf("%d",&first);
printf("enter second number\n");
temp=first;
first=second;
second=temp;
printf("now the swapped first number is %d\n",first);
printf("now the swapped second number is %d\n",second);
return 0;
}
